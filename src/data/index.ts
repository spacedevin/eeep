import {
  createTable,
  addTableColumn,
  setTableStyle
} from '../tables/tables';

import {
  createPivotTable,
  addPivotField
} from '../pivottables/pivottables';

import {
  createDataTable,
  setRowInput,
  setColumnInput
} from '../datatables/datatable';

export {
  createTable,
  addTableColumn,
  setTableStyle,
  createPivotTable,
  addPivotField,
  createDataTable,
  setRowInput,
  setColumnInput
};